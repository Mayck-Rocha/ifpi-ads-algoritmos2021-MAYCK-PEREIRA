const prompt = require('prompt-sync')();

//Entrada

const lado = Number(prompt('Insira o valor do lado: '))

//Cálculo da área do quadrado

const área = (lado * lado)

//saída

console.log('A área do quadrado é ',área)